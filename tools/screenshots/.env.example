# Copy this file to `.env` and fill in real values. `.env` is gitignored,
# so your credentials never end up in the repository.
#
# These are read by the authenticate() hook in screenshots.config.js.

BLUECHERRY_USER=you@example.com
BLUECHERRY_PASS=your-password
